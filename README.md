# AI 招聘数字员工团队（HR Agent Team）

## 简介

本项目是一款基于大模型的多智能体招聘应用，打造一支由 5 名"数字员工"组成的 AI 招聘团队：招聘分析师、简历猎手、面试官（含命题与阅卷）、测评背调员、offer 与入职管家。

用户只需给出一句模糊的招聘诉求（如"招个会做数据的人"），整个团队即可自主接力，端到端完成**需求澄清 → 岗位描述生成 → 简历筛选 → 线上笔试 → 题库化面试 → 测评背调 → 综合评分 → offer 与入职计划**的完整招聘闭环，交付结构化岗位描述、简历初筛报告、笔试卷与笔试成绩单、面试题库与面试评分表、综合评分排名、测评背调报告、offer 草案与 30/60/90 天入职计划等 **16 类可直接验收**的工作成果——报告与网页为 HTML、汇总为 CSV，全部是真实文件，页面内可直接预览与下载。

系统后端采用 Java / Spring Boot 自研轻量多智能体框架实现：5 位数字员工实现统一的 Agent 接口、各有明确的技能与输出契约，由调度层用会话状态机串联成**可回退**的接力流程（任何一步都能退回重跑，只清该步之后的成果）。简历解析与成果落盘是**真实的工具调用**——简历猎手由模型通过 function-calling 自主决定调用简历解析工具，各环节再用 doc_writer 把结论落成真实文件；而分数、排名、权重、薪酬明细与录入门禁则**全部由确定性代码计算**，同一组输入永远得到同一个结果，可复现、可追溯；关键节点（offer 是否接受）保留人工确认。产品以网页形式交付、打开即用（前端 Vue 3 + Vite，后端 Spring Boot），并已容器化部署上线（Docker + nginx + HTTPS）。

## 面向群体

- **中小企业 HR 与招聘负责人**：缺乏专职招聘团队，招聘环节多、重复劳动重，负担不起全套专业 HR 系统；
- **业务部门负责人**：需要把模糊的用人诉求，快速变成可执行、可推进的完整招聘方案；
- **人力资源服务与就业机构**：需要批量、标准化、可追溯地处理大量招聘事务。

## 开发背景

1. **行业趋势**：AI 正从"对话式助手"迈向"自主执行"的智能体时代，业界呼唤能真正交付工作成果的"数字员工"——本项目正是对这一趋势的产品化探索。

2. **真实痛点**：一次招聘横跨需求分析、简历筛选、面试、测评、offer、入职 6 大环节，中小企业既缺人手也缺标准，招聘质量难保证、过程难沉淀、成果难验收。

3. **技术条件**：大模型的工具调用与多智能体编排能力已趋成熟。本项目自研轻量 Agent 框架（统一 Agent 接口 + 工具注册表 + OpenAI 兼容的模型客户端），以真实可运行的 5-Agent 协同系统验证"人人可拥有的 AI 数字员工"这一命题；并用统一的技能注册表与数据契约解决多智能体之间的耦合与风格一致性问题。

---

## 项目结构

```
.
├── hr-agent-team/              后端：Spring Boot 3.2.5（Java 17）
│   ├── src/main/java/com/hragent/
│   │   ├── agent/              5 名数字员工：analyst / scout / interviewer / assessor / concierge(+examiner 命题阅卷)
│   │   ├── dispatcher/         调度层：会话状态机、逐人并发、综合评分与录用筛选
│   │   ├── common/             公共口径：Scores 评分、Texts/Jsons 工具、DeepSeekClient、NoticeHtml
│   │   ├── tool/               doc_writer 等工具（成果落盘）、简历解析
│   │   └── web/                REST 接口（/api/recruit/**、/api/files/**）
│   ├── src/main/resources/     配置（端口 8083）、印章图片（随 jar 走）
│   └── demo/                   **手写的演示样例**（报告样例、预览图），不是运行输出
│
├── hr-agent-team_vue/          前端：Vue 3 + Vite + Element Plus
│   ├── src/components/         环节页、字段版式、表单、成果清单
│   ├── src/stores/recruit.js   步骤机 + 全流程状态（含逐人并发、思考过程）
│   ├── src/utils/              成果编排、评分版式、offer 规则、滚动跟随等纯函数
│   ├── 前端开发交接文档.md        开发过程中踩过的坑与约定（60+ 条）
│   └── API接口文档.md           前后端接口契约
│
├── deploy/                     线上部署（宝塔 + Docker）
│   ├── Dockerfile.api/.web     两个镜像：Spring Boot / nginx
│   ├── nginx.conf              前端容器：静态文件 + /api 反代（含 600s 超时）
│   ├── docker-compose.yml      一条命令起两个容器
│   ├── up.sh                   服务器上执行：bash deploy/up.sh
│   ├── 自检.mjs                 构建前自检路径/端口/反代口径（node deploy/自检.mjs）
│   └── 部署说明.md              从零到域名的完整步骤
│
├── plan.md                     开发记录：每一轮需求、做法、验证结果
├── 文档库/                     比赛材料：项目介绍、技术方案、技能注册表等
├── _演示成果/                   演示用成品（生成归档/ 里是历次跑出来的产物，可清理）
├── 插入/                       早期成员协作时插入的源码副本（历史材料，不参与构建）
└── mysql-upgrade/              独立的数据库迁移子项目（与本应用无关）
```

## 本地运行

```bash
# 1) 后端（端口 8083，模型 Key 可在页面「设置」里填，也可设环境变量 DEEPSEEK_API_KEY）
cd hr-agent-team && mvn spring-boot:run

# 2) 前端（Vite，默认直连 http://localhost:8083）
cd hr-agent-team_vue && npm install && npm run dev
```

运行产生的成果文件默认写到 `hr-agent-team/out/`（可用环境变量 `HR_DEMO_DIR` 改到别处）。

## 线上部署

见 **`deploy/部署说明.md`**：Ubuntu + 宝塔 + Docker，域名与 SSL 在宝塔里配，容器只绑 127.0.0.1。

```bash
# 本地生成部署包（默认"产物模式"：本地构建好 app.jar + dist，服务器只组装镜像，几十秒完成）
node deploy/打包.mjs

# 传上去后一条命令构建启动
cd <服务器上的目录> && bash deploy/up.sh
```

想要"只传源码、在服务器上构建"就用 `node deploy/打包.mjs --source`（服务器需能拉 Maven/npm 依赖）。
